# What to do next

Read with [ROADMAP.md](ROADMAP.md) and [GAP_ANALYSIS.md](GAP_ANALYSIS.md). Current pin is `0.6.0`.

## Now (SPEC-017)

1. Keep phones and web on `/api/v0` only. No HTML commands from a phone. No WebView of `/app` sold as the app.
2. Android: food and shelter requests the same way as the signed-in ride path, or leave the cards inert. Do not fake a booking.
3. Worker: stop requiring a person-chosen tenant on JSON sign-in. Resolve the enrolled email on the server.
4. Confirm or correct the fail-closed Wave C defaults if the owner wants a different rule.
5. Record SPEC-017 completion in [STATUS.md](STATUS.md) only after the owner accepts the evidence. Slices being built is not that record.

## Not now

- Production host, real Veteran data, app-store listing.
- Spending the $300 cloud credits. That needs a named evidence run and a spend log.
- Inventing chat, duty matching, or dashboard math.
- Inventing SMS, food vendors, payment, or restore-time numbers.
- Merging stale iOS drafts that predate the HTTPS / JSON Case-open work already on `suas-ios` main.

## Then (SPEC-018)

Only after the owner closes the launch decisions and attaches test evidence: hosting, mail/SMS, database, job queue, counsel on the privacy notice, and any live vendor. Gates move only with that evidence.

## After launch (SPEC-019)

Measured changes from a real pilot. Not funding paperwork. Funding readiness is the D-037 overlay and stays off this chain.
